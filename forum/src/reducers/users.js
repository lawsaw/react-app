const initialState = [
    {
        name: 'admin',
        level: '10',
    }, {
        name: 'saw',
        level: '100500',
    }
];

export function userReducer(state = initialState, action) {

    switch (action.type) {
        default:
            return state
    }

}